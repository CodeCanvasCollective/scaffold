import { defineConfig } from 'vite';
import react from '@vitejs/plugin-react';

export default defineConfig({
  plugins: [react()],
{{#if features.testing}}
  test: {
    globals: true,
    environment: 'jsdom',
    setupFiles: [],
  },
{{/if}}
});
